{
    "name": "result-management-root",
    "version": "1.0.0",
    "description": "Root management for Result Management System",
    "scripts": {
        "install:all": "npm install && cd client && npm install && cd ../server && npm install",
        "dev:server": "cd server && npm run dev",
        "dev:client": "cd client && npm run dev",
        "dev": "npx concurrently -n \"SERVER,CLIENT\" -c \"bgBlue,bgMagenta\" \"npm run dev:server\" \"npm run dev:client\"",
        "build": "cd client && npm install && npm run build",
        "postinstall": "cd server && npm install"
    },
    "dependencies": {
        "@supabase/supabase-js": "^2.89.0",
        "cors": "^2.8.5",
        "dotenv": "^17.2.3",
        "express": "^5.2.1",
        "zod": "^3.24.1"
    },
    "devDependencies": {
        "@types/cors": "^2.8.17",
        "@types/express": "^4.17.21",
        "@types/node": "^22.10.2",
        "concurrently": "^8.2.2",
        "typescript": "~5.3.3"
    }
}